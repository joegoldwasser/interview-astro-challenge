/**
 * Task 3: Async Data Fetching with Loading & Error States
 *
 * Build a component that fetches "featured posts" from an async API
 * and displays them with proper loading and error handling.
 *
 * Requirements:
 * - Call fetchFeaturedPosts() on mount (imported from cms-helpers.ts)
 * - Show a loading indicator while the data is being fetched
 * - Show an error message if the fetch fails
 * - Once loaded, display the featured posts as styled cards
 * - Include a "Refresh" button that re-fetches the data
 *
 * Note: fetchFeaturedPosts() has a built-in 1.5 second delay to simulate
 * a real API call.
 */

export default function FeaturedPosts() {
  // TODO: Implement async data fetching with loading/error states
  return <div>Task 3: Build the featured posts component here</div>;
}
